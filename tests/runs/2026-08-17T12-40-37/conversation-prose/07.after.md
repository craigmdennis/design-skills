The byte-hash comparison found an error in my own reader. The specification states that the comparison detects corpus corruption. One directory contains four images. My reader selected the image from the directory listing instead of the name that the frontmatter declares, so it imported the wrong image and reported no error. A cross-check between two independent paths to the same answer finds errors on *either* side.
