Your build stopped at `prebuild`, before the compile step. `scripts/check-drafts.js` scans `src/content/posts/*.md` for `TKTK` and exits 1 if it finds any. `TKTK` is the standard placeholder for "text to come". The script found one at `src/content/posts/measuring-adoption.md:41`.

That file's frontmatter states `draft: false`, so the post is marked ready to publish. The check reports this combination: a post declared finished that still contains a placeholder.

The same script passed on the previous commit, so the change arrived in this commit. You added the `TKTK`, or you changed `draft` from `true` to `false` while the placeholder was still in the file. `git diff HEAD~1 -- src/content/posts/measuring-adoption.md` shows which.

Two options:

1. Write the missing text at line 41 and remove the `TKTK`. Use this option if the post is ready and the placeholder is the last text missing.
2. Set `draft: true` in the frontmatter. Use this option if the post is unfinished. The build then excludes the post until you set `draft` back to `false`.

Do not skip `prebuild`. Do not make the pattern in the script less strict. The fix at line 41 takes two minutes, and either of those changes permits a placeholder to reach production later.
