Your build stopped at `prebuild`, before the compile step ran. `scripts/check-drafts.js` scans `src/content/posts/*.md` for `TKTK` — the standard placeholder for "text to come" — and exits 1 if it finds any. The script found one at `src/content/posts/measuring-adoption.md:41`.

That file's frontmatter says `draft: false`, so the post is marked ready to publish. The check exists to report that exact combination: a post declared finished that still contains a placeholder.

The same script passed on the previous commit, so the change is in this commit. Either the `TKTK` was just added, or `draft` was changed from `true` to `false` while the placeholder was still there. Run `git diff HEAD~1 -- src/content/posts/measuring-adoption.md` to see which.

You can do one of two things:

1. Write the missing text at line 41 and remove the `TKTK`. This is correct if the post is ready and this is the last gap.
2. Set `draft: true` in the frontmatter. This is correct if the post is unfinished. The build then excludes the post until you set `draft: false` again.

Don't skip `prebuild`, and don't make the pattern in the script less strict. Either change lets a placeholder reach production later, and the fix here takes two minutes.
