9 of 14 routes are migrated with passing tests.

3 more are migrated but failing, all on the same thing: handlers call `req.body`, which hono's context doesn't provide. That's one fix pattern (`await c.req.json()` or equivalent) applied three times, so I expect it to go quickly.

2 aren't started. Both use multer for file uploads, and there's no hono equivalent in the project's dependencies. That needs a decision from you before I go further — add a middleware dependency, hand-roll multipart parsing, or leave those two on express for now.

Caveat on the passing tests: I've only run the tests for files I touched, not the full suite. So I can't tell you whether the migration broke anything downstream.
