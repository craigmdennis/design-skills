That repo doesn't have those files, so I'm going on your description alone.

`check-drafts.js` is a publish guard: it treats `TKTK` as "I haven't written this bit yet" and refuses to build if that marker survives into something publishable. It's doing its job. `measuring-adoption.md` says `draft: false`, so the build treats it as ready to ship, but line 41 still has a placeholder in it.

The interesting part is that it passed on the previous commit, which means one of two things changed in this one: either the `TKTK` was added to a post that was already marked published, or the `TKTK` was already there and you flipped `draft: true` to `draft: false`. Those have different fixes, so it's worth knowing which:

```
git diff HEAD~1 -- src/content/posts/measuring-adoption.md
```

If the placeholder is new, write the missing sentence at line 41 and rebuild. If the draft flag is what changed, you published a post you hadn't finished — set it back to `draft: true` until line 41 is real.

Either way, don't reach for the third option of loosening the check. A guard that fires the first time it catches something is a guard working correctly.
