Your build stopped at `prebuild`, before anything got compiled. `scripts/check-drafts.js` scans `src/content/posts/*.md` for `TKTK` — the standard placeholder for "text to come" — and exits 1 if it finds any. It found one at `src/content/posts/measuring-adoption.md:41`.

That file's frontmatter says `draft: false`, so it's marked ready to publish. The check exists precisely to catch that combination: a post declared finished that still has a placeholder in it. The guard is working as intended.

Since the same script passed on the previous commit, the change came in with this one — either the `TKTK` was just added, or `draft` was flipped from `true` to `false` while the placeholder was still there. `git diff HEAD~1 -- src/content/posts/measuring-adoption.md` will tell you which.

Two ways forward:

1. Write the missing text at line 41 and remove the `TKTK`. Right if the post is genuinely ready and this is the last gap.
2. Set `draft: true` in the frontmatter. Right if the post isn't finished; it'll be excluded from the build until you flip it back.

Don't reach for skipping `prebuild` or loosening the pattern in the script — that trades a two-minute fix for a placeholder shipping to production later.
